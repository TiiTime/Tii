# Tii

Portfolio for **BraveSaver** and **Glossa**.

**Live:** https://tiitime.github.io/Tii/

Language: `?lang=de` or `?lang=en` — toggle DE / EN in the header.

Crawlers: `robots.txt` · `sitemap.xml`  
Google: Search Console → URL-Präfix `https://tiitime.github.io/Tii/` → Sitemap `sitemap.xml` einreichen.

```powershell
python -m http.server 8765
```
